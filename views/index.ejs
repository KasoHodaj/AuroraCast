<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AuroraCast Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css"> <!-- Fixed CSS Path -->
</head>
<body>
    <div class="container">
        <h1 class="text-center mt-5">Weather Dashboard</h1>

        <!-- Search Form -->
        <form action="/" method="get" class="d-flex justify-content-center my-4">
            <input type="text" name="city" class="form-control w-50" placeholder="Search City" required>
            <button type="submit" class="btn btn-primary ms-2">Search</button>
        </form>

        <!-- Display Weather Data -->
        <% if (weather) { %>
            <div class="text-center">
                <h2>Weather in <%= city %></h2>
                <p>Current Temperature: <%= weather.current.temp %>°C</p>
                <p>Condition: <%= weather.current.weather[0].description %></p>
            </div>

            <!-- Daily Forecast -->
            <h3 class="mt-4">Daily Forecast</h3>
            <ul class="list-group">
                <% if (weather.daily) { %>
                    <% weather.daily.slice(0, 5).forEach(day => { %>
                        <li class="list-group-item">
                            <p>Day Temp: <%= day.temp.day %>°C</p>
                            <p>Night Temp: <%= day.temp.night %>°C</p>
                            <p>Condition: <%= day.weather[0].description %></p>
                        </li>
                    <% }); %>
                <% } else { %>
                    <p>No daily forecast data available.</p>
                <% } %>
            </ul>
        <% } else { %>
            <p class="text-center">No weather data available. Try searching for a city!</p>
        <% } %>
    </div>

    <!-- Bootstrap JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
